% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vocabulary.R
\name{.vocabListConcepts}
\alias{.vocabListConcepts}
\title{List concepts with server-side pagination}
\usage{
.vocabListConcepts(
  handle,
  domain = NULL,
  vocabulary = NULL,
  concept_class = NULL,
  standard = NULL,
  valid = NULL,
  offset = 0L,
  limit = 100L,
  order = "concept_id"
)
}
\arguments{
\item{handle}{CDM handle}

\item{domain}{Character; filter by domain_id}

\item{vocabulary}{Character; filter by vocabulary_id}

\item{concept_class}{Character; filter by concept_class_id}

\item{standard}{Character; filter by standard_concept value (e.g. "S")}

\item{valid}{Logical; TRUE = only valid (invalid_reason IS NULL)}

\item{offset}{Integer; number of rows to skip}

\item{limit}{Integer; page size (capped at 1000)}

\item{order}{Character; column to order by (must be a selectable column)}
}
\value{
List with \code{rows} (data frame), \code{total_count},
  \code{offset}, and \code{limit}
}
\description{
Browse the \code{concept} catalog filtered by the standard facets and paged
with OFFSET/LIMIT, mirroring Athena's paged concept list. This lifts the
500-row cap of \code{\link{.vocabSearchConcepts}} by letting the caller walk
the result in pages. Returns a list with the current \code{rows} page and the
\code{total_count} of matching concepts (one extra COUNT query) so the client
can render pagination controls.
}
\details{
\code{LIMIT ... OFFSET ...} is portable across the realistically-targeted
analytic dialects (PostgreSQL, SQLite/DuckDB, MySQL, Spark, BigQuery,
Snowflake, Redshift); it is rendered directly rather than via the TOP
convention, which has no offset notion. The page size is still capped to keep
a single response bounded.
}
\keyword{internal}
