% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_catalog.R
\name{.omopBinarySmd}
\alias{.omopBinarySmd}
\title{Standardized mean difference between two binary prevalences}
\usage{
.omopBinarySmd(p1, p2)
}
\arguments{
\item{p1}{Numeric vector; prevalence in arm 1 (from banded counts).}

\item{p2}{Numeric vector; prevalence in arm 2 (from banded counts).}
}
\value{
Numeric vector of SMDs (NA where either side is suppressed).
}
\description{
For a binary covariate the standardized mean difference is
\code{(p1 - p2) / sqrt((p1 (1 - p1) + p2 (1 - p2)) / 2)} (the classic
Cohen-style SMD on the pooled binomial SD). Computed elementwise; returns
\code{NA} where either prevalence is \code{NA} (a suppressed arm) or the
pooled SD is zero. Both prevalences MUST already be derived from BANDED
counts so the SMD never rests on un-banded inputs.
}
\keyword{internal}
