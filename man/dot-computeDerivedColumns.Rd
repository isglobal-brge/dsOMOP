% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extraction.R
\name{.computeDerivedColumns}
\alias{.computeDerivedColumns}
\title{Compute person-level derived columns (age, sex, observation duration)}
\usage{
.computeDerivedColumns(
  handle,
  derived_specs,
  person_ids = NULL,
  cohort_table = NULL
)
}
\arguments{
\item{handle}{CDM handle.}

\item{derived_specs}{List of derived column specs, each with at least
\code{$kind} and \code{$name}.}

\item{person_ids}{Integer vector of person IDs to restrict to, or
\code{NULL} for all persons.}

\item{cohort_table}{Character; name of cohort temp table (used for
index-date age computation), or \code{NULL}.}
}
\value{
A \code{data.frame} with \code{person_id} and one column per
  derived spec, or \code{NULL} if no specs or no data.
}
\description{
Fetches data from the \code{person} table (and \code{observation_period}
if needed) and computes derived columns for each person. Supports three
kinds of derived columns:
\describe{
  \item{age}{Numeric age computed as \code{reference_year - year_of_birth}.}
  \item{sex_mf}{Character "M" or "F" mapped from \code{gender_concept_id}.}
  \item{obs_duration}{Integer days between observation period start and end.}
}
}
\keyword{internal}
