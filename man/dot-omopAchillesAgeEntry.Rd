% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pack_achilles_dist.R
\name{.omopAchillesAgeEntry}
\alias{.omopAchillesAgeEntry}
\title{Build one \code{dsomop:achilles.<id>} age-distribution entry (ACH-G)}
\usage{
.omopAchillesAgeEntry(analysis_id, description, domain, tables, fn)
}
\arguments{
\item{analysis_id}{Integer Achilles analysis id (e.g. 206L).}

\item{description}{Character; the analysis label.}

\item{domain}{Character; OMOP domain key (listing + table resolution).}

\item{tables}{Character vector of CDM tables the fn reads (deps WARN).}

\item{fn}{The kind="r" compute function(handle, ctx, params).}
}
\value{
An \code{omop_analysis_entry}.
}
\description{
Shared builder for every ACH-G entry: unit="dist", count_cols="count_value",
min_max=TRUE (so the gate strips min/max age and masks stats below
nfilter_dist), cohort-OPTIONAL (accepts_cohort/accepts_tables TRUE,
max_tables 1L, requires_cohort FALSE so an un-scoped run computes cohort-wide
preserving whole-DB Achilles semantics), kind="r", and
meta=list(adapter="achilles_live", analysis_id=<id>) — the analysis_id is a
LABEL only (never used to read a results table).
}
\keyword{internal}
