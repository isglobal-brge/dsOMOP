% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manipulate.R
\name{omopMergeDS}
\alias{omopMergeDS}
\title{Merge two omop.table frames on the person key (Assign)}
\usage{
omopMergeDS(x, y, by = "person_id", type = "inner")
}
\arguments{
\item{x, y}{Server-side omop.table data.frames (resolved from symbols by
DataSHIELD).}

\item{by}{Character vector of join columns. Must be the person key
(\code{"person_id"} / \code{"subject_id"}); may not include any other
protected/identifier column.}

\item{type}{Character; \code{"inner"} (default) or \code{"left"}.}
}
\value{
The joined omop.table (re-assigned to the caller's symbol).
}
\description{
Inner or left join of two server-side, token-keyed omop.table frames. Joining
is restricted to the person/subject token so a client cannot use \code{by}
to pivot on a protected identifier other than the person key. The joined
result is re-gated on its distinct-person token count (fail-closed) before it
can become a symbol.
}
