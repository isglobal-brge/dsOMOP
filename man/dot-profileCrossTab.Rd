% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profiling.R
\name{.profileCrossTab}
\alias{.profileCrossTab}
\title{Build a disclosure-safe 2-way (optionally stratified) cross-tabulation}
\usage{
.profileCrossTab(
  handle,
  table,
  row_col,
  col_col,
  count_mode = "persons",
  row_concept_ids = NULL,
  col_concept_ids = NULL,
  cohort_table = NULL,
  stratify_by = NULL,
  band_margins = FALSE
)
}
\arguments{
\item{handle}{CDM handle.}

\item{table}{Character; table name.}

\item{row_col}{Character; row categorical column.}

\item{col_col}{Character; column categorical column.}

\item{count_mode}{Character; "persons" (distinct person_id) or "records".}

\item{row_concept_ids, col_concept_ids}{Optional integer vectors restricting
the levels of the row/column axes.}

\item{cohort_table}{Character; cohort temp table to scope the population.}

\item{stratify_by}{Character; optional 3rd categorical column for stratified
chained 2-way tables.}

\item{band_margins}{Logical; attach banded (never exact) margins.}
}
\value{
For a plain call: a named list \code{{row_col, col_col, count_mode,
  row_levels, col_levels, counts (NA-masked matrix), suppressed}}. For a
  stratified call: \code{{stratified = TRUE, stratify_by, strata = <named
  list of per-level protected tables>}}.
}
\description{
Server-side engine for \code{\link{omopCrossTabDS}}. Cross-tabulates two
categorical columns of an OMOP table, counting either distinct persons
(default) or records, then applies primary + iterative complementary
small-cell suppression on the dense matrix (see \code{\link{.crossTabSuppress}}).
Exact margins are never returned. When \code{stratify_by} is supplied, an
INDEPENDENT protected 2-way table is produced for each stratum level (see
section 7 of the disclosure spec); the unstratified total is never returned.
}
\keyword{internal}
