% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vocabulary.R
\name{.vocabDistinctMeta}
\alias{.vocabDistinctMeta}
\title{Distinct values from a vocabulary metadata table}
\usage{
.vocabDistinctMeta(handle, table, fallback_col)
}
\arguments{
\item{handle}{CDM handle}

\item{table}{Character; dedicated table name (e.g. "vocabulary")}

\item{fallback_col}{Character; concept column to distinct on if absent}
}
\value{
Data frame
}
\description{
Shared helper for the \code{vocabulary} / \code{domain} / \code{concept_class}
listers. Prefers the dedicated vocabulary table when present (returning its
full descriptive rows); otherwise falls back to \code{SELECT DISTINCT} on the
corresponding \code{concept} column so the lister still works on a minimal
vocabulary load.
}
\keyword{internal}
