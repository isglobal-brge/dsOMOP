% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interface.R
\name{omopOhdsiResultContractDS}
\alias{omopOhdsiResultContractDS}
\title{Get the pooling contract for a physical OHDSI result table (Aggregate)}
\usage{
omopOhdsiResultContractDS(omop_symbol, table_name, tool_id = NULL)
}
\arguments{
\item{omop_symbol}{Character; the OMOP handle symbol}

\item{table_name}{Character; physical OHDSI result-table name}

\item{tool_id}{Character; optional tool identifier for registry lookup}
}
\value{
Named list containing contract version, canonical tool and table
  identifiers, and the typed pooling contract
}
\description{
Resolves the exact physical result table inside the server-authorised OHDSI
results namespace and returns its reviewed, typed pooling contract. The
contract is inert metadata with a closed output schema; it contains no SQL,
database-qualified names, credentials, or raw result values. Unreviewed or
malformed physical tables fail closed.
}
\examples{
\dontrun{
contract <- omopOhdsiResultContractDS("omop", "cohort_count")
}
}
