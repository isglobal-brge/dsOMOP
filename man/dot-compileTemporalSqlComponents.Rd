% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal_sql.R
\name{.compileTemporalSqlComponents}
\alias{.compileTemporalSqlComponents}
\title{Compile SQL-first temporal output components}
\usage{
.compileTemporalSqlComponents(
  handle,
  cohort_table,
  table,
  concept_filter = NULL,
  bin_width = 30L,
  window_start = -365L,
  window_end = 0L,
  analyses = "binary",
  filters = NULL,
  output_type = "temporal_covariates",
  grain = NULL,
  time_origin = NULL
)
}
\arguments{
\item{handle}{CDM handle.}

\item{cohort_table}{Cohort table with subject/start/end columns.}

\item{table}{Person-keyed OMOP event table.}

\item{concept_filter}{Optional public concept-ID set.}

\item{bin_width}{Positive integer bin width in days.}

\item{window_start, window_end}{Inclusive integer offsets from index.}

\item{analyses}{Non-empty subset of \code{binary} and \code{count}.}

\item{filters}{Optional reviewed row-filter tree.}

\item{output_type}{\code{temporal_covariates} or \code{person_period}.}

\item{grain, time_origin}{Required as \code{episode}/\code{index} for a
person-period output.}
}
\value{
An internal component plan containing SQL or small public data frames.
}
\description{
Compiles the large components of \code{temporal_covariates} and
\code{person_period} without executing or materializing them. The returned
queries expose only generated episode/time/covariate identifiers and relative
day bins. Every cohort episode must have exactly one OMOP observation period
covering its index date. Events outside that period are excluded. Absolute
dates and OMOP source-row identifiers remain inside nested queries and are
never projected by a component query.
}
\details{
The caller must execute every entry in \code{validations} before streaming a
component. In particular, \code{min_persons} preserves DataSHIELD's
qualifying-population gate, and \code{max_concepts} bounds a data-derived
covariate dictionary when no public concept filter was supplied.
}
\keyword{internal}
