% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interface.R
\name{omopAnalysisRunAssignDS}
\alias{omopAnalysisRunAssignDS}
\title{Run a unified analysis catalog assign-mode loader (Assign)}
\usage{
omopAnalysisRunAssignDS(
  omop_symbol,
  name,
  params = list(),
  scope = NULL,
  combine = "union"
)
}
\arguments{
\item{omop_symbol}{Character; the OMOP handle symbol}

\item{name}{Character; the entry name (pack-prefixed stable id)}

\item{params}{Named list; parameter values for the entry}

\item{scope}{Optional cohort reference and/or \code{omop.table} symbol(s)}

\item{combine}{Character; "union" (default) or "intersect" when scope has
multiple sources}
}
\value{
The server-side assignment result (data stays on the server)
}
\description{
Assign-mode counterpart to \code{\link{omopAnalysisRunDS}} for QueryLibrary
entries whose template \code{mode} is \code{"assign"} (server-side loaders
whose result stays on the server, never returned to the client). Resolves and
runs the entry through the same fail-closed run path; because the data is not
returned, the aggregate disclosure gate does not apply, but parameter
sanitization and scope re-gating still do.
}
\examples{
\dontrun{
loaded <- omopAnalysisRunAssignDS("omop", "dsomop:condition.occurrence_load")
}
}
