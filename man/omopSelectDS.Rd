% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manipulate.R
\name{omopSelectDS}
\alias{omopSelectDS}
\title{Keep a subset of columns of an omop.table (Assign)}
\usage{
omopSelectDS(x, cols)
}
\arguments{
\item{x}{A server-side omop.table data.frame.}

\item{cols}{Character vector of columns to keep.}
}
\value{
The projected omop.table (re-assigned to the caller's symbol).
}
\description{
Projects a server-side omop.table to \code{cols} (intersected with the
frame's columns), always retaining the person/subject token so the result
stays a valid, joinable omop.table. Selecting columns never lowers the
person count; the gate is re-run only for uniformity with the other verbs.
}
