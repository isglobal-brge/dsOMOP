% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extraction.R
\name{.extractTable}
\alias{.extractTable}
\title{Extract a table as a data frame with all filters applied}
\usage{
.extractTable(
  handle,
  table,
  columns = NULL,
  concept_filter = NULL,
  person_ids = NULL,
  time_window = NULL,
  cohort_table = NULL,
  translate_concepts = TRUE,
  representation = "long",
  feature_specs = NULL,
  representation_grain = "person",
  block_sensitive = TRUE,
  temporal = NULL,
  date_handling = NULL,
  add_cohort_date = FALSE,
  filters = NULL,
  concept_col = NULL,
  visit_filter = NULL
)
}
\arguments{
\item{handle}{CDM handle}

\item{table}{Character; table name}

\item{columns}{Character vector; columns to select}

\item{concept_filter}{Numeric vector; concept IDs}

\item{person_ids}{Numeric vector; person IDs}

\item{time_window}{Named list}

\item{cohort_table}{Character; cohort temp table name}

\item{translate_concepts}{Logical; replace concept IDs with names}

\item{representation}{Character; "long", "wide", "features", or "sparse".}

\item{feature_specs}{Named list; for features mode}

\item{representation_grain}{Character; aggregation unit for wide/features,
either "person" or "episode". Episode grain requires a cohort-anchored
index window and preserves one row per cohort_row_id.}

\item{block_sensitive}{Logical; block sensitive columns}

\item{temporal}{List; temporal filtering spec}

\item{date_handling}{List; date handling spec}

\item{filters}{List; optional custom filter DSL tree (see
\code{\link{.compileSelect}}); validated fail-closed and ANDed in.}

\item{concept_col}{Character; optional concept-scoping column override
passed through to \code{\link{.compileSelect}}.}

\item{visit_filter}{List; optional visit-linkage filter passed through to
\code{\link{.compileSelect}}.}
}
\value{
Data frame
}
\description{
Extract a table as a data frame with all filters applied
}
\details{
In wide mode, an explicit \code{concept_filter} is also the output
  column contract: every requested concept is materialized even when no
  qualifying row is observed. This keeps a closed concept set structurally
  identical across federated sites.
}
\keyword{internal}
