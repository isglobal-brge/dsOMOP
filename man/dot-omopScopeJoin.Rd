% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_catalog.R
\name{.omopScopeJoin}
\alias{.omopScopeJoin}
\title{INNER JOIN clause onto the scoped cohort (or "" when cohort-wide)}
\usage{
.omopScopeJoin(ctx, alias, person_col = "person_id")
}
\arguments{
\item{ctx}{Run-path ctx.}

\item{alias}{Character; the alias of the table being joined to the cohort.}

\item{person_col}{Character; that table's person column (default person_id).}
}
\value{
list(join = "...", cohort = "<validated table or NULL>").
}
\description{
Every native diagnostic restricts its population to \code{ctx$scoped_cohort}
the same way: an INNER JOIN on \code{subject_id}. NULL scope -> "" (cohort-wide).
The cohort identifier is re-validated here as defence-in-depth.
}
\keyword{internal}
