% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pack_achilles_obsperiod.R
\name{.omopAchillesDistRow}
\alias{.omopAchillesDistRow}
\title{Summarise a per-person value vector into one Achilles dist row}
\usage{
.omopAchillesDistRow(v, label_cols = list())
}
\arguments{
\item{v}{Numeric vector of per-person values (already NA-stripped).}

\item{label_cols}{Named list of scalar stratum labels (may be empty).}
}
\value{
One-row data frame, or an empty frame when \code{v} is empty.
}
\description{
Mirrors the per-covariate summarisation in \code{\link{.omopCovariateContinuous}}:
\code{count_value} = number of persons, \code{min_value}/\code{max_value}
(emitted but stripped by the gate), \code{avg_value}, \code{stdev_value}, and
the p10/p25/median/p75/p90 quantiles (\code{stats::quantile} type 7) — NEVER
native 0%/100% (those are min/max). Optional \code{label_cols} (a named list)
are prepended as stratum identifier columns.
}
\keyword{internal}
