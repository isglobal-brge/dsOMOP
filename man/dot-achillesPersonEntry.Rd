% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pack_achilles_person.R
\name{.achillesPersonEntry}
\alias{.achillesPersonEntry}
\title{Build one native person-domain Achilles catalog entry}
\usage{
.achillesPersonEntry(analysis_id, description, domain, tables, fn)
}
\arguments{
\item{analysis_id}{Integer Achilles analysis id (the stable id suffix).}

\item{description}{Human-readable description.}

\item{domain}{OMOP domain string for listing ("person").}

\item{tables}{Character vector of CDM tables the fn reads (for the deps WARN).}

\item{fn}{The kind="r" compute function(handle, ctx, params).}
}
\value{
An \code{omop_analysis_entry}.
}
\description{
Shared builder: every entry in this group is unit="person", count_cols=
"count_value", cohort-OPTIONAL (accepts_cohort/accepts_tables TRUE, max_tables
1L, requires_cohort FALSE so an un-scoped run computes cohort-wide preserving
whole-DB Achilles semantics), kind="r", and carries
meta=list(adapter="achilles", analysis_id=<id>) — the analysis_id is a LABEL
only (never used to read a results table).
}
\keyword{internal}
