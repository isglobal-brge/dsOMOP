% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/disclosure.R
\name{.dropSuppressed}
\alias{.dropSuppressed}
\title{Drop rows already flagged suppressed, and remove the flag column}
\usage{
.dropSuppressed(df, col = "suppressed")
}
\arguments{
\item{df}{Data frame possibly containing a logical \code{suppressed} column}

\item{col}{Character; the flag column name (default \code{"suppressed"})}
}
\value{
\code{df} with suppressed rows and the flag column removed
}
\description{
"No hints" at the source: a result table must never be RETURNED carrying a
\code{suppressed} marker. A suppressed row reveals that a rare
concept/value/bin EXISTS (with too few persons), which is itself disclosive.
This removes such rows entirely and drops the now-redundant flag column, so
the suppressed marker never leaves the server.
}
\keyword{internal}
