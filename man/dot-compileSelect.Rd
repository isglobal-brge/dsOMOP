% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extraction.R
\name{.compileSelect}
\alias{.compileSelect}
\title{Compile a SQL SELECT for a table extraction}
\usage{
.compileSelect(
  handle,
  table,
  columns = NULL,
  concept_filter = NULL,
  person_ids = NULL,
  time_window = NULL,
  cohort_table = NULL,
  limit = NULL,
  block_sensitive = TRUE,
  temporal = NULL,
  add_cohort_date = FALSE,
  filters = NULL,
  concept_col = NULL,
  visit_filter = NULL
)
}
\arguments{
\item{handle}{CDM handle}

\item{table}{Character; table name}

\item{columns}{Character vector; columns to select (NULL = all)}

\item{concept_filter}{Numeric vector; concept IDs to filter on}

\item{person_ids}{Numeric vector; person IDs to restrict to}

\item{time_window}{Named list with start_date, end_date, date_column}

\item{cohort_table}{Character; name of cohort temp table}

\item{limit}{Integer; max rows}

\item{block_sensitive}{Logical; block sensitive columns (default TRUE)}

\item{temporal}{List; temporal filtering spec (index_window, calendar, etc.)}

\item{add_cohort_date}{Logical; if TRUE, add cohort start/end date columns
from the cohort table to the output.}

\item{filters}{List; an optional custom filter DSL tree (AND/OR of leaves,
see \code{\link{.compileFilter}}). Validated fail-closed by
\code{\link{.assertCustomFilterSafe}} and ANDed with the other predicates.}

\item{concept_col}{Character; optional override of the column the
\code{concept_filter} IN-list scopes (default: the table's domain concept
column). Lets a caller scope by \code{unit_concept_id},
\code{*_type_concept_id}, or \code{value_as_concept_id}.}

\item{visit_filter}{List; optional \code{list(concept_ids = ...)} restricting
events to visits of those \code{visit_concept_id} values via a join on
\code{visit_occurrence_id} to \code{visit_occurrence}.}
}
\value{
Character; compiled SQL statement
}
\description{
Compile a SQL SELECT for a table extraction
}
\keyword{internal}
