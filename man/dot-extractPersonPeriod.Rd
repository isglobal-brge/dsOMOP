% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/person_period.R
\name{.extractPersonPeriod}
\alias{.extractPersonPeriod}
\title{Extract a complete episode-by-period panel}
\usage{
.extractPersonPeriod(
  handle,
  cohort_table,
  table,
  concept_filter = NULL,
  bin_width = 30L,
  window_start = -365L,
  window_end = 0L,
  analyses = c("binary"),
  grain,
  time_origin,
  filters = NULL
)
}
\arguments{
\item{handle}{CDM handle.}

\item{cohort_table}{Cohort table containing episode dates.}

\item{table}{OMOP event table.}

\item{concept_filter}{Concept IDs to include.}

\item{bin_width}{Positive integer bin width in days.}

\item{window_start, window_end}{Inclusive integer offsets from index.}

\item{analyses}{Subset of \code{binary} and \code{count}.}

\item{grain}{Must be \code{"episode"}.}

\item{time_origin}{Must be \code{"index"}.}

\item{filters}{Optional reviewed row-filter tree.}
}
\value{
A list containing \code{personPeriods}, sparse
  \code{temporalCovariates}, \code{covariateRef}, \code{timeRef}, and the
  complete episode-to-person \code{personRef}.
}
\description{
Builds on the temporal-covariate contract, adding the episode/time bins that
intersect the unique OMOP observation period covering each index date.
\code{startDay}/\code{endDay} retain the requested bin boundaries, while
\code{observationStartDay}/\code{observationEndDay} delimit the observed part
of that bin and \code{daysObserved} is inclusive. Covariate rows remain
sparse: absence of a \code{(rowId, timeId, covariateId)} row means zero only
when the corresponding \code{personPeriods} row exists. Absolute index/event
dates and source row identifiers are never returned. This descriptive panel
does not infer a risk set from cohort end or death; use survival or
counting-process outputs for explicit time-at-risk analyses.
}
\keyword{internal}
