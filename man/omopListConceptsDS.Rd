% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interface.R
\name{omopListConceptsDS}
\alias{omopListConceptsDS}
\title{List concepts with pagination (Aggregate)}
\usage{
omopListConceptsDS(
  omop_symbol,
  domain = NULL,
  vocabulary = NULL,
  concept_class = NULL,
  standard = NULL,
  valid = NULL,
  offset = 0L,
  limit = 100L,
  order = "concept_id"
)
}
\arguments{
\item{omop_symbol}{Character; the OMOP handle symbol}

\item{domain}{Character; filter by domain_id}

\item{vocabulary}{Character; filter by vocabulary_id}

\item{concept_class}{Character; filter by concept_class_id}

\item{standard}{Character; filter by standard_concept value (e.g. "S")}

\item{valid}{Logical; TRUE keeps only currently-valid concepts}

\item{offset}{Integer; rows to skip (page start)}

\item{limit}{Integer; page size (capped at 1000)}

\item{order}{Character; column to order by (default "concept_id")}
}
\value{
List with \code{rows} (data frame), \code{total_count},
  \code{offset}, and \code{limit}
}
\description{
Browses the \code{concept} catalog filtered by domain, vocabulary, concept
class, standard status, and validity, paged with OFFSET/LIMIT. Lifts the
500-row cap of \code{\link{omopSearchConceptsDS}} for catalog browsing,
mirroring Athena's paged concept list. Returns the current page plus the
total matching count. Reference-data reader; not disclosure-gated.
}
\examples{
\dontrun{
page <- omopListConceptsDS("omop", vocabulary = "SNOMED", limit = 100)
}
}
