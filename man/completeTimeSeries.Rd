% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08_longitudinal.R
\name{completeTimeSeries}
\alias{completeTimeSeries}
\title{Time Series Completion for Longitudinal Data}
\usage{
completeTimeSeries(table, mergeColumn, conceptIdColumn)
}
\arguments{
\item{table}{A data frame containing longitudinal data}

\item{mergeColumn}{A character string specifying the entity identifier column (usually "person_id")}

\item{conceptIdColumn}{A character string specifying the concept ID column name}
}
\value{
A data frame with expanded rows to include all date combinations for each entity
}
\description{
Expands a longitudinal dataset to include a row for each date within the observed range
for each entity, even when no data exists for that date. This standardizes the dataset
so all entities have observations at the same time points, making analysis and merging easier.
}
\details{
Complete Time Series for Longitudinal Data

The function performs the following operations:
\enumerate{
\item Date Identification:
\itemize{
\item Automatically detects the date column based on the concept ID column
\item Ensures proper date type casting
}
\item Range Determination:
\itemize{
\item Identifies the full range of dates across all entities
\item Creates a complete set of all possible entity-date combinations
}
\item Data Expansion:
\itemize{
\item Joins the original data with the complete set of combinations
\item Adds rows with NA values for missing observations
\item Preserves all original data values
}
}

This function is essential for:
\itemize{
\item Standardizing longitudinal datasets
\item Ensuring consistent time points across all entities
\item Facilitating time-based comparisons
\item Preparing data for time series analysis
}
}
\examples{
\dontrun{
# Example with patient observation data
observations <- data.frame(
  person_id = c(1, 1, 2),
  observation_concept_id = c("weight", "weight", "weight"),
  observation_date = as.Date(c("2023-01-01", "2023-03-01", "2023-02-15")),
  value = c(70, 72, 65)
)

# Complete the time series
complete_observations <- completeTimeSeries(
  table = observations,
  mergeColumn = "person_id",
  conceptIdColumn = "observation_concept_id"
)
# Results in a table with rows for all dates between Jan 1 and Mar 1
# for both person_id 1 and 2, with NA values where no data existed
}

}
