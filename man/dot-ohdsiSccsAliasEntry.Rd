% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ohdsi_pack_sccs.R
\name{.ohdsiSccsAliasEntry}
\alias{.ohdsiSccsAliasEntry}
\title{Build one dsomop:ohdsi.sccs.<table> alias onto a canonical SCCS entry}
\usage{
.ohdsiSccsAliasEntry(table_name, builder)
}
\arguments{
\item{table_name}{Character; the legacy SCCS result table (the \code{<table>}
in the id); a LABEL only (never read).}

\item{builder}{Zero-arg function returning the canonical SCCS entry.}
}
\value{
An \code{omop_analysis_entry} keyed (by the registrar) on its id.
}
\description{
Keeps the legacy OHDSI id as a thin delegate to the canonical
\code{dsomop:sccs.<metric>} entry (the B1 alias pattern): inherits the
canonical compute fn, disclosure spec, params, dependencies and scope, and
carries the OHDSI labels (tool_id, table_name, alias_target) for listing only.
The canonical entry already computes live and gates correctly, so no compute
logic is duplicated here.
}
\keyword{internal}
