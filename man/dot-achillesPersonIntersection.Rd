% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pack_achilles_person.R
\name{.achillesPersonIntersection}
\alias{.achillesPersonIntersection}
\title{Cross-domain distinct-person intersection count}
\usage{
.achillesPersonIntersection(handle, ctx, domain_tables)
}
\arguments{
\item{handle}{CDM handle.}

\item{ctx}{Run-path ctx (carries ctx$scoped_cohort).}

\item{domain_tables}{Character vector of CDM table names every counted person
must appear in.}
}
\value{
One-row aggregate-only data.frame with \code{count_value}.
}
\description{
Single-scalar \code{COUNT(DISTINCT person_id) AS count_value} over the scoped
person population (cohort-wide when un-scoped) restricted to persons present in
EVERY supplied domain table (Achilles 2000..2003: e.g. >=1 Dx AND >=1 Rx). The
restriction is a chain of \code{person_id IN (SELECT person_id FROM <domain>)}
predicates — no concept ids, no source values. Self-gates the scoped+filtered
population before materialising. Returns an aggregate-only one-row frame.
}
\keyword{internal}
