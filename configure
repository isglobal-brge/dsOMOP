#!/bin/sh
# dsOMOP post-install configuration script
# Runs automatically during R CMD INSTALL on Unix systems.
#
# The Rock/DataSHIELD base image already includes all required R database
# packages (RPostgreSQL, RSQLite, RMariaDB, odbc, arrow, DBI).
# This script only creates the marker directory for caching state.

LOG_PREFIX="[dsOMOP configure]"
MARKER_DIR="${DSOMOP_MARKER_DIR:-/var/lib/dsomop}"

log_info() { echo "${LOG_PREFIX} $*"; }

mkdir -p "${MARKER_DIR}" 2>/dev/null || true
log_info "Ready."

exit 0
